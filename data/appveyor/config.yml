version: "{build}"
clone_depth: 1

environment:
  matrix:
    - BUILDENV: ubuntu
      SRCML: v0.9.5
      CC: ccache clang
      APPVEYOR_BUILD_WORKER_IMAGE: Ubuntu
    - BUILDENV: ubuntu
      SRCML: v1.0
      CC: ccache gcc
      APPVEYOR_BUILD_WORKER_IMAGE: Ubuntu
    - BUILDENV: macos
      CC: ccache clang
      APPVEYOR_BUILD_WORKER_IMAGE: macOS

cache:
  - $HOME/.ccache

build_script:
  - data/appveyor/$BUILDENV.sh
